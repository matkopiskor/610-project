import { AppFooter, AppHeader, AppContent } from './containers';

import './App.less';

export const App = () => {
    return (
        <>
            <AppHeader />
            <AppContent />
            <AppFooter />
        </>
    );
};
